import React from 'react'
import Navbar from '../../components/shared/Navbar'

type Props = {}

const Home = (props: Props) => {
  return (
    <div>
        <Navbar/>
    </div>
  )
}

export default Home